export function updateConvert (state, convert) {
  state.convert = convert
}

export function updatePdftk (state, pdftk) {
  state.pdftk = pdftk
}

export function updatePdftotext (state, pdftotext) {
  state.pdftotext = pdftotext
}

export function updateTesseract (state, tesseract) {
  state.tesseract = tesseract
}
